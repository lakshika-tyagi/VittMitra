# VittMitra Cross-Service Integration Tests

This directory contains end-to-end integration tests validating flows across the Frontend, Backend, AI services, and Database layers.

## Structure
- Service-specific unit tests reside within their respective directories (e.g., `backend/tests/`).
- End-to-end integration workflows (such as full onboarding -> eligibility calculation -> explanation flow) are tested here.
